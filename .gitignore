# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor directories
.vscode/
.idea/
*.swp
*.swo
*.bak
*~

# Netlify local dev
.netlify/

# Node (if ever added)
node_modules/
npm-debug.log*

# Word source documents (resume) — only the exported PDF is published
*.docx

# ── Secrets / credentials (defense-in-depth) ──────────────────────────────
# Environment files
.env
.env.*
!.env.example

# Private keys & certificates
*.key
*.pem
*.p12
*.pfx
*.cer
*.crt
*.der
id_rsa
id_rsa.*
id_dsa
id_dsa.*
id_ecdsa
id_ecdsa.*
id_ed25519
id_ed25519.*
*.ppk

# Cloud / tool credentials
.aws/
.azure/
.gcloud/
.kube/config
credentials.json
service-account*.json
*.tfvars
!*.tfvars.example
terraform.tfstate*

# Common secret-laden files
secrets.*
*.secret
*.secrets
config.local.*
